language: csharp  
sudo: required  
dist: xenial  
mono: none
#dotnet: 3.0.100-preview8-013656
addons:
  snaps:
  - name: dotnet-sdk
    classic: true
    channel: latest/beta

script:
  - sudo snap alias dotnet-sdk.dotnet dotnet
  - dotnet --version
  - dotnet test ./SwitchTest
